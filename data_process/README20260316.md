这几个脚本的作用依次是：
1. customer_log_step1.py: 处理/data/aiimport_1119/客户信息log.csv，只抽取其中我们想要的字段，按时间戳排序，按月划分，把结果放到/data/processed/customer_log下
2. validate_customer_log.py: 验证数据完备性，即所有客户信息.csv中的id都能在客户信息log.csv中找到对应baseid
3. login_step1.py: 处理/data/aiimport_1119/登录信息.csv，抽取我们想要的字段，按时间戳排序，按月划分，把结果放到/data/processed/login下
4. 同1和3
5. 同1和3
6. 同1和3
7. 没啥用，不用运行
8. 没啥用，不用运行
9. 同1和3
10. 同1和3
11. 同1和3
12. 同1和3

13. 整合1-12的结果（/data/processed），按时间戳排序，形成每月的事件数据流并放到/data/processed/events下（见PPT中的示例）。注意，该脚本所需运行时间【极长】（你看下/data/processed/events的文件修改时间，大概运行了5天半），可以对脚本简单调试，正式运行务必以nohup的方式在服务器上运行，命令示例如下：
```bash
nohup python integrate_events.py > integrate_events.log 2>&1 &
```
14. 抽取几条数据形成示例文件

# 接下来需要做什么？
1. 对公司信息log.csv、还款计划.csv、客诉工单.csv、消保案件工单.csv、黑名单log.csv也进行处理，并加入到事件数据流中。为加速处理流程并方便后续测试，建议把数据按日划分而不要按月划分了（不要删除原本的processed，你可以把你新预处理后的数据放到processed_v2下）
注意：在处理黑名单log.csv时，建议把黑产中介标签.csv中的数据也纳入进来，为每个黑名单用户添加更丰富的信息
